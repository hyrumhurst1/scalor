[project]
name = "scalor-backend"
version = "0.1.0"
description = "Scalor — empirical Big-O complexity analyzer (static v1)."
requires-python = ">=3.11"
readme = "../README.md"

[tool.ruff]
line-length = 100
target-version = "py311"
